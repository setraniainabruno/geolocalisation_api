services:
  web:
    build: .
    container_name: geolocalisation
    ports:
      - "3723:3723"
    volumes:
      - .:/app
    depends_on:
      - mongo
    restart: always
  mongo:
    container_name: mongo
    image: mongo:7
    ports:
      - "27017:27017"
    volumes:
      - ./data:/data/db
